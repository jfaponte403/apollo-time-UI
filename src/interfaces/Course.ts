export interface Course {
    id: string;
    classroom_id: string;
    subject_id: string;
    degrees_id: string;
    teacher_id: string;
    name: string;
    is_active: boolean;
    created_at: string;
}
